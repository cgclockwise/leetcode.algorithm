# leetcode.algorithm
class solution{
  public int removeDuplicate(int[]nums){
    int number = 0;
    for(int i=0;i<=nums.length;i++){
      if(nums[i] != nums[number] ){
        number++;
        nums[number] = nums[i];
        }
      }
      number+=1;
      return number
    }
 }
